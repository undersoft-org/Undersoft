@using System.ComponentModel.DataAnnotations
@using Undersoft.SDK.Rubrics
@using Undersoft.SDK.Invoking
@using Undersoft.SDK.Proxies

@inherits ViewItem<TMenu>

@typeparam TMenu where TMenu : class, IOrigin, IInnerProxy

<div class="generic top-menu">
    <FluentStack Orientation="Orientation.Horizontal" VerticalAlignment="VerticalAlignment.Center" HorizontalAlignment="HorizontalAlignment.Right" HorizontalGap="5">
        @foreach (var rubric in Data.Rubrics)
        {
            <div id="@($"{Model.CodeNo}{@rubric.CodeNo}")" role="button">
                @if (IsSingle && !ShowLabels)
                {
                    <FluentIcon Value="@(new Icons.Regular.Size20.LineHorizontal4())" Color="Color.FillInverse" Title="Categories" />
                }
                else
                {
                    <FluentLabel Typo="Typography.Body" Weight="FontWeight.Bold" Color="Color.Lightweight">
                        @rubric.DisplayName
                    </FluentLabel>
                }
            </div>
            <div>
                <GenericMenu Position="HorizontalPosition.Right" Rubric="@rubric" AnchorId="@($"{Model.CodeNo}{@rubric.CodeNo}")" Data="@(Data[rubric.RubricId])" Anchored="true" Style="@("margin-top:14px;")" />
            </div>
        }
    </FluentStack>
</div>




